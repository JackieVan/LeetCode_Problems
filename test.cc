#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <memory.h>
#include <queue>

using namespace std;

int main()
{
    std::queue<int> queue;
    std::vector<int> vec(33);
    string str = "123 44 22";
    std::cout << vec.size() << vec[22] << "\n";
    return 0;
}