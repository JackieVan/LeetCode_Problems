#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <memory.h>

using namespace std;

int main()
{
    std::vector<int> vec(33);
    std::cout << vec.size() << vec[22] << "\n";
    return 0;
}